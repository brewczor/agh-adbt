--before
DELETE FROM x;

--BEGIN TRANSACTION ISOLATION LEVEL Repeatable READ;

--T1: 
BEGIN TRANSACTION ISOLATION LEVEL Repeatable READ;
--T2: 
BEGIN TRANSACTION ISOLATION LEVEL Repeatable READ;
SELECT * FROM x;
--0 rows
--T1: 
INSERT INTO x VALUES (1, 2);
--T2:
SELECT * FROM x;
--0 rows
--T1: 
COMMIT;
--T2: 
SELECT * FROM x;
--0 rows
ROLLBACK;
